# c-project
#include<stdio.h>
int main()
{
    int n;
    printf("Enter a positive integer: ");
    scanf("%d", &n);
    //int result=fact(n);
    printf("factorial of %d=%d ",n,fact(n));

}
  int fact(int n)
  {
      if(n==1)
        return 1;
      else
        return n*fact(n-1);
  }
